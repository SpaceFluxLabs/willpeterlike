var game = new Phaser.Game(800,600,Phaser.CANVAS, 'phaser', {preload: preload, create: create, update: update});

function preload() {
	// Stuff happens here before the canvas is created.
}

function create() {
	// Stuff happens here the instance the canvas is created.
}

function update() {
	// This function gets called per certain time interval. How often, need to check documentation
}